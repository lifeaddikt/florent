---
import Link from "./Link.astro"
const { prevUrl, nextUrl } = Astro.props;


---
<div class="container">
    <div class="row">
        PAGINATION
        {/* 
            prevUrl && (
                <Link 
                    text="Previous"
                    href={prevUrl}
                />
            )
        }
        {
            nextUrl && (
                <Link 
                    text="Next"
                    href={nextUrl}
                />
            )
        */}
    </div>
</div>
